<style>
    .dashboard-wrapper {
        position: relative;
        left: 0;
        margin-left: 29px;
        min-height: 870px !important;
    }
</style>


<div class="row mt-5">
    <div class="col-md-12" style="margin-top:12pc;"> 
        <h1 class="text-center">Please rate your experience?</h1>

        <form style="width: 20%; margin: 0 auto; margin-right: 44%;">

            <div class="row">

                <div class="form-group" style="width: 20%; margin: 0 auto;">
                    <button type="button" class="btn btn-primary">Customer</button>
                </div>

                <div class="form-group" style="width: 20%; margin: 0 auto;">
                    <button type="button" class="btn btn-primary">Non-Customer</button>
                </div>


            </div>

        </form>
    </div>
</div>

<script>
    const timer = document.getElementById('txt');
    var hr = 0;
    var min = 0;
    var sec = 0;
    var stoptime = true;

    function timerCycle() {

        sec = parseInt(sec);
        min = parseInt(min);
        hr = parseInt(hr);

        sec = sec + 1;

        if (sec == 60) {
            min = min + 1;
            sec = 0;
        }
        if (min == 60) {
            hr = hr + 1;
            min = 0;
            sec = 0;
        }

        if (sec < 10 || sec == 0) {
            sec = '0' + sec;
        }
        if (min < 10 || min == 0) {
            min = '0' + min;
        }
        if (hr < 10 || hr == 0) {
            hr = '0' + hr;
        }

        timer.innerHTML = hr + ':' + min + ':' + sec;

        setTimeout("timerCycle()", 1000);

    }

    timerCycle();
</script>